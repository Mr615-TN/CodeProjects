struct Product {
    url: Option<String>,
    image: Option<String>,
    name: Option<String>,
    price: Option<String>
}

fn main() {
    let mut products: Vec<Products> = Vec::new();
    let response = req
}
